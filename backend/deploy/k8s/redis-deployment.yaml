apiVersion: apps/v1
kind: Deployment
metadata:
  name: area-redis
  labels:
    app: area-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: area-redis
  template:
    metadata:
      labels:
        app: area-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: area-redis
spec:
  selector:
    app: area-redis
  ports:
  - port: 6379
    targetPort: 6379

